//! Individual command implementations (cmd_status, cmd_search, cmd_get, etc.)
//!
//! Each command lives in its own file for maintainability.

pub mod status;
// pub mod search;   // TODO: extract cmd_search + FTS helpers
// pub mod get;
// pub mod collection;
// pub mod ls;
// pub mod mcp;
