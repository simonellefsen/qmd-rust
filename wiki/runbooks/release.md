---
type: runbook
tags:
  - qmd-rust/wiki
  - release
  - packaging
updated: 2026-05-23
---

# Release Process for QMD-Rust

This runbook covers how we package and distribute the Rust binary, maintain the changelog, and support installation via Nix, Homebrew, and other methods.

## Changelog Rules

We follow the same release standards as the original project:

- All user-visible changes go under `## [Unreleased]` in [CHANGELOG.md](../../CHANGELOG.md) **as you make them**.
- The release script (or manual process) renames `[Unreleased]` → `[X.Y.Z] - YYYY-MM-DD`.
- Credit external contributors with `#NNN (thanks @username)`.
- GitHub releases should roll up the full minor series.

See the release skill in `skills/release/` and `scripts/release.sh` (in the archived original) for the exact workflow. We will adapt the release tooling for Rust as we stabilize.

## Packaging the Rust Binary

The goal is to provide a small, static-ish binary with minimal runtime dependencies (only system SQLite + optional sqlite-vec extension).

### Current State (as of this writing)

- `cargo build --release` produces a working `target/release/qmd`.
- We have a basic Nix flake (`flake.nix`) that builds the package and provides a `devShell`.
- No official Homebrew formula or cargo-dist setup yet.

### Recommended Future Packaging Stack

We should converge on a modern Rust-friendly release pipeline:

1. **cargo-dist** (strongly recommended)
   - Generates GitHub Actions that build binaries for Linux (x86_64 + aarch64), macOS (Intel + Apple Silicon), and Windows.
   - Automatically creates GitHub Releases with proper archives.
   - Can generate a Homebrew formula and push it to a `homebrew-qmd` tap.
   - Handles `cargo install` from the tap as a fallback.
   - Excellent changelog + announcement support.

2. **GitHub Releases** as the source of truth
   - Pre-built static binaries for the major platforms.
   - Users can download and put on `$PATH`, or use the install script generated by cargo-dist.

3. **Nix**
   - The existing `flake.nix` already works.
   - We can expose it via `nix run github:simonellefsen/qmd-rust` once we have a proper flake output.
   - Home-manager module is already present in the flake (can be improved).

4. **Homebrew** options (the user specifically asked about these)

   Here are the realistic options, ranked from best to worst for a new Rust CLI:

   **Best option (modern standard): cargo-dist + custom tap**
   - `cargo dist` can generate and maintain a `homebrew-qmd` tap automatically.
   - Formula installs the pre-built binary from the GitHub release (very fast for users, no compilation).
   - Users do:
     ```bash
     brew tap simonellefsen/qmd
     brew install qmd
     ```
   - This is what most serious Rust CLIs do in 2025–2026 (examples: `just`, `uv`, `ripgrep` tools, `starship`, etc.).

   **Good fallback: Simple custom tap with cargo install**
   - A minimal Formula.rb that does:
     ```ruby
     class Qmd < Formula
       desc "Secure on-device search for markdown knowledge bases"
       homepage "https://github.com/simonellefsen/qmd-rust"
       url "https://github.com/simonellefsen/qmd-rust/archive/refs/tags/v0.2.0.tar.gz"
       # ... sha256 ...
       depends_on "rust" => :build
       def install
         system "cargo", "install", *std_cargo_args
       end
     end
     ```
   - Downside: first install compiles from source (slow on macOS).

   **Official homebrew-core**
   - Only worth it once the project has significant adoption (hundreds of stars, regular releases, clear maintenance).
   - Formula would be similar to the custom tap but lives in the main Homebrew repo.
   - High bar; not recommended as the first Homebrew strategy.

   **Quick & dirty (not recommended long-term)**
   - Users can do `brew install --HEAD` against a tap, or use a raw GitHub URL formula. These break easily and should only be used for testing.

## Proposed Release Pipeline (Target)

1. Tag a release (`git tag v0.2.0`).
2. GitHub Action (powered by `cargo-dist`) builds binaries for all supported platforms + generates the Homebrew formula.
3. GitHub Release is created with proper notes (extracted from CHANGELOG).
4. The Homebrew tap is automatically updated (or we do a manual PR if we start simple).
5. Nix users get the update via the flake.
6. Optional: publish a static binary tarball + install script for people who don't want a package manager.

## Action Items / Next Steps

- Adopt `cargo-dist` (add it via `cargo dist init` and the generated GitHub workflow).
- Decide on the Homebrew tap name (`homebrew-qmd` or `qmd` under the simonellefsen org).
- Update `flake.nix` to produce a proper package + improve the Home Manager module.
- Write a small install script that works without Homebrew/Nix (`curl ... | sh`).
- Keep the changelog discipline (Unreleased → versioned on release).

Once the above is in place, installing the Rust qmd will be as easy as the original Node version was, while keeping the security and performance benefits of a native binary.

---

*This page should be updated whenever the release or packaging strategy changes.*